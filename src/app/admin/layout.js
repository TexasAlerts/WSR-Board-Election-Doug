export const metadata = {
  title: 'Admin | Doug Charles for Prosper Town Council',
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
